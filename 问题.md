## 当前问题描述

当前 Search-Free PMI 模型仅使用容量损失 \(L_{\text{cap}}\) 训练，希望学习

\[
H \rightarrow \Theta(H)
\]

从而针对不同信道自适应选择双流预编码波束。

目前的核心异常是：**模型在不同信道样本上的输出 \(\Theta\) 变化非常小，趋向于输出一组近似固定的双流波束，而没有实现预期的逐样本动态寻优。**

该现象同时存在于训练集和验证集，因此不能简单归因于泛化不足。例如当前水平波束参数的圆周标准差只有约

\[
\mathrm{circ\_std}(\theta_{h1})\approx0.03\sim0.04,\qquad
\mathrm{circ\_std}(\theta_{h2})\approx0.03\sim0.05.
\]

但对不同信道执行离散码本搜索后，最优空间波束具有明显的样本差异，例如 20 个样本得到

\[
\mathrm{circ\_std}
=
[0.1866,\ 0.2305,\ 0.2240,\ 0.3020]
\]

对应 \((\theta_{h1},\theta_{v1},\theta_{h2},\theta_{v2})\)。

因此可以确认：

> 信道确实存在明显的逐样本最优 PMI 差异，但当前神经网络没有学到这种映射。

---

## 当前原因分析

### 1. Backbone 表征坍缩不是主要原因

此前已经检查 Transformer 中间特征：

- 修复了过大的位置编码；
- 修复了 FiLM 导致的层间方差爆炸；
- Backbone 输出特征方差恢复至正常量级；
- Token 间相似度和有效特征秩均正常。

因此目前没有证据表明不同信道在 Backbone 中已经被映射成相同特征。

即：

\[
H_1\neq H_2
\Rightarrow
z(H_1)\neq z(H_2)
\]

但最终仍有

\[
\Theta(H_1)\approx\Theta(H_2).
\]

问题更可能发生在“利用特征进行波束决策”的优化阶段。

### 2. 输出头结构也不是主要原因

已经尝试：

- 原始 `4096 → 512 → 128 → 6` Flatten Head；
- `[CLS] → 512 → 6`；
- 两个 stream 使用独立 MLP。

训练结果均没有明显改变。

因此当前现象不能主要归因于 Head 参数量过大、两流梯度串扰或 Flatten 操作。

### 3. 大 Batch 梯度平均不是根本原因

将训练集缩小至 200 个样本，并将

\[
B=1024 \rightarrow B=4
\]

后，训练容量可以达到约 816，但训练集和验证集的 \(\Theta\) 标准差仍然很小，验证容量约为 758。

如果大 Batch 中不同样本梯度互相抵消是主要原因，那么极小 Batch 应明显增加输出的样本差异，但实验没有观察到这一现象。

因此 Batch averaging 可能有影响，但不是当前问题的主要根因。

### 4. 当前最可能的问题：容量优化空间高度多峰，导致共同 Basin 锁定

重新绘制单个样本的 \(256\times256\) 双流码本容量图后可以看到，容量 landscape 并不是简单的平坦区域，而具有明显的：

- 多峰结构；
- 周期结构；
- 局部高容量区域；
- 双流交换对称性。

例如 Sample 112 的最优解包括

\[
(\mathbf b_A,\mathbf b_B)
\]

以及完全交换后的

\[
(\mathbf b_B,\mathbf b_A),
\]

两者容量完全相同。

同时还存在多个距离很远但容量非常接近的局部峰，例如 Top-1 为 824.61 bits，而其他完全不同位置的组合仍可达到 822～824 bits。

因此当前连续优化实际面对的是一个具有大量局部 basin 的目标：

\[
C(H,\Theta).
\]

网络初始化时，不同样本的 \(\Theta(H)\) 本身比较接近。在共享网络参数训练过程中，大量样本可能首先进入同一个较好的容量 basin，之后梯度只能在该 basin 内进行局部优化，而难以跨越容量谷进入各个样本真正的最优 basin。

最终形成：

\[
\Theta(H)\approx\Theta_{\mathrm{common}}
\]

即一个近似“通用预编码器”，而不是

\[
\Theta(H)\approx\Theta^\star(H).
\]

### 5. 连续回归与离散 PMI 选择之间可能存在天然矛盾

真正目标实际上接近

\[
\Theta^\star(H)
=
\arg\max_{\Theta\in\mathcal C}C(H,\Theta),
\]

其中 \(\mathcal C\) 是离散码本。

虽然 \(C(H,\Theta)\) 对连续 \(\Theta\) 可以求导，但最优码本索引随信道变化可能发生离散跳变，因此

\[
H\rightarrow\Theta^\star(H)
\]

本身不一定是平滑映射。

当前采用连续参数回归并通过局部梯度最大化容量，很可能容易找到稳定的局部高容量解，却缺乏跨 basin 搜索能力。

---

## 当前最可能的机制

目前证据更支持以下链路：

\[
\text{不同 }H
\rightarrow
\text{Backbone 保留不同特征}
\]

但训练初期

\[
\Theta(H_1)\approx\Theta(H_2)\approx\cdots
\]

随后由于容量 landscape 多峰且具有较强局部吸引域：

\[
\Theta(H_n)
\rightarrow
\text{同一或相邻容量 basin}
\]

最终形成

\[
\boxed{
\text{样本相关特征仍然存在，但输出决策被锁定在共同的局部最优区域}
}
\]

因此当前最值得验证的是 **continuous capacity optimization 是否存在明显的 basin locking / initialization dependence**。

---

## 下一步测试方案

### Test 1：单样本直接优化 \(\Theta\)

完全移除神经网络，对约 20 个已经完成 exhaustive search 的信道分别建立独立的可训练参数

\[
\Theta_n.
\]

直接进行

\[
\min_{\Theta_n}-C(H_n,\Theta_n).
\]

每个样本分别测试三种初始化：

1. 当前网络输出 \(\Theta_{\mathrm{net}}(H_n)\)；
2. 所有样本使用相同固定初始化；
3. 每个样本进行 10～20 次随机初始化。

记录：

\[
C_{\mathrm{final}},\qquad
C_{\mathrm{search}},\qquad
C_{\mathrm{search}}-C_{\mathrm{final}}
\]

以及最终 \(\Theta\)。

#### 判断标准

如果网络初始化只能达到较低容量，而 random restart 经常能够逼近 exhaustive-search 最优值，则说明：

\[
\boxed{\text{局部 basin 和初始化依赖是主要问题}}
\]

如果即使大量 random restart 也很难接近离散搜索最优值，则说明：

\[
\boxed{\text{当前连续参数化本身不适合通过局部梯度寻找码本最优解}}
\]

---

### Test 2：Shared-\(\Theta\) 对照实验

对同样的 20 个信道，只设置一组共享参数

\[
\Theta_{\mathrm{shared}}
\]

优化

\[
\max_{\Theta_{\mathrm{shared}}}
\frac{1}{N}\sum_n C(H_n,\Theta_{\mathrm{shared}}).
\]

比较：

\[
\Theta_{\mathrm{shared}}
\]

与当前网络输出均值以及对应容量。

如果 Shared-\(\Theta\) 最终与网络输出非常接近，则可以进一步确认：

> 当前神经网络实际上主要学习到了 dataset-level 的通用局部最优 precoder，而没有真正执行 sample-dependent PMI selection。

---

## 当前建议

现阶段不建议继续优先调整 Transformer 层数、Head 规模或 Batch Size。

应先通过上述直接 \(\Theta\) 优化实验判断问题究竟属于：

\[
\boxed{\text{优化方法导致的 basin locking}}
\]

还是

\[
\boxed{\text{continuous regression 参数化与离散 PMI selection 不匹配}}
\]

确认这一点之后，再决定是否需要转向 multi-start optimization、soft codebook selection、分类式 PMI prediction 或其他离散化训练方式。
