## Test 4：样本间梯度冲突诊断

### 1. Theta-space 梯度

#### 原理

对第 \(i\) 个样本，定义其输出参数梯度为：

$$
g_i^{\Theta}
=
\frac{\partial L_i}{\partial \Theta_i}.
$$

该梯度描述：在不考虑神经网络共享参数的情况下，当前样本希望其预编码参数 \(\Theta_i\) 朝哪个方向调整。

通过样本间梯度余弦相似度：

$$
\cos(g_i,g_j)
=
\frac{
g_i^Tg_j
}{
\|g_i\|\|g_j\|
}
$$

可以判断不同信道对 \(\Theta\) 的优化方向是否一致。

同时定义梯度抵消率：

$$
R=
\frac{
\left\|
\frac1N\sum_i g_i
\right\|
}{
\frac1N\sum_i\|g_i\|
}.
$$

\(R\) 越小，说明不同样本梯度平均后剩余的有效方向越弱。但 \(R\) 小不一定意味着强反向冲突，也可能来自大量近似正交的梯度，因此需要结合余弦相似度共同判断。

#### 数据比较

测试集：

- 平均梯度范数：64.43
- 平均梯度范数的向量均值：11.79
- 梯度抵消率：

$$
R_{\text{test}}=0.1830
$$

- 平均余弦相似度：-0.0008
- 中位数余弦相似度：-0.0042
- 负相关比例：50.10%
- 强负相关比例：

$$
P(\cos<-0.5)=26.93\%
$$

- 强正相关比例：

$$
P(\cos>0.5)=27.48\%
$$

训练集：

- 平均梯度范数：14.34
- 平均梯度范数的向量均值：3.27
- 梯度抵消率：

$$
R_{\text{train}}=0.2282
$$

- 平均余弦相似度：0.0318
- 中位数余弦相似度：0.0429
- 负相关比例：47.77%
- 强负相关比例：

$$
P(\cos<-0.5)=20.49\%
$$

- 强正相关比例：

$$
P(\cos>0.5)=24.01\%
$$

#### 分析

测试集的平均 Theta-space 梯度范数约为训练集的：

$$
\frac{64.43}{14.34}\approx4.5
$$

倍。

说明测试样本当前输出距离适合自身的容量驻点明显更远，而训练样本已经得到较充分适配。这与此前单样本直接优化中“测试集可继续提升约 50–90 bits，而训练集提升明显较小”的结果一致。

同时，测试集中约 26.9% 的样本对具有：

$$
\cos(g_i,g_j)<-0.5,
$$

说明大量不同信道确实需要明显相反的 \(\Theta\) 调整方向；另有约 27.5% 的样本对强同向。平均余弦接近 0 并不意味着没有结构，而更可能是多个不同梯度方向簇相互抵消后的结果。

因此可以认为：

$$
\boxed{
\text{不同信道在 }\Theta\text{-space 中具有明显不同甚至冲突的优化方向}
}
$$

---

### 2. Final Head 参数梯度

#### 原理

对输出头最后一层参数 \(\psi_{\text{head}}\)，第 \(i\) 个样本产生的梯度为：

$$
g_i^{\text{head}}
=
\frac{\partial L_i}{\partial \psi_{\text{head}}}.
$$

它反映为了改善某个样本的容量，共享输出层参数应该如何更新。

若最后一层可写为：

$$
r_i=Wz_i+b,
$$

则其权重梯度满足：

$$
\frac{\partial L_i}{\partial W}
=
\delta_i z_i^T,
$$

其中：

$$
\delta_i=
\frac{\partial L_i}{\partial r_i}.
$$

对于两个样本，其权重梯度内积近似满足：

$$
\left\langle
g_i^W,g_j^W
\right\rangle
\propto
(\delta_i^T\delta_j)(z_i^Tz_j).
$$

因此，即使两个样本在输出空间中的优化方向存在冲突，只要其隐藏特征 \(z_i,z_j\) 差异较大甚至近似正交，映射到 Final Head 参数空间后的梯度也可能接近正交。

#### 数据比较

测试集：

- 平均梯度范数：482.01
- 平均梯度范数的向量均值：67.52
- 梯度抵消率：

$$
R_{\text{test}}^{\text{head}}=0.1401
$$

- 平均余弦相似度：0.0004
- 中位数余弦相似度：-0.0002
- 负相关比例：50.00%
- 强负相关比例：

$$
P(\cos<-0.5)=0.10\%
$$

- 强正相关比例：

$$
P(\cos>0.5)=0.20\%
$$

训练集：

- 平均梯度范数：144.48
- 平均梯度范数的向量均值：30.62
- 梯度抵消率：

$$
R_{\text{train}}^{\text{head}}=0.2119
$$

- 平均余弦相似度：0.0099
- 中位数余弦相似度：0.0009
- 负相关比例：48.81%
- 强负相关比例：

$$
P(\cos<-0.5)=0.10\%
$$

- 强正相关比例：

$$
P(\cos>0.5)=0.89\%
$$

#### 分析

Final Head 中，训练集和测试集的样本间梯度余弦相似度几乎都集中在 0 附近。

虽然约一半样本对的 cosine 为负，但真正强烈反向的：

$$
\cos<-0.5
$$

仅约 0.1%。

因此不能认为 Final Head 中存在严重的“正面冲突”。更准确地说，这些梯度主要表现为：

$$
\boxed{
\text{高维参数空间中的近似正交}
}
$$

而不是：

$$
g_i^{\text{head}}\approx-g_j^{\text{head}}.
$$

测试集的：

$$
R=0.1401
$$

虽然较小，但对于 \(N=64\) 个近似独立、正交方向的梯度，理论上也会有：

$$
R\approx\frac1{\sqrt{64}}=0.125.
$$

因此该低 \(R\) 更可能主要来自高维梯度的正交性，而不是强烈的负向抵消。

这说明网络隐藏特征实际上能够将不同样本的优化需求映射到不同的参数更新方向。换言之：

$$
\boxed{
\text{Final Head 本身尚未表现出严重的直接梯度冲突}
}
$$

---

### 3. Train/Test 梯度幅值差异

#### 原理

梯度范数反映当前位置的局部优化驱动力：

$$
\|g_i\|
=
\left\|
\frac{\partial L_i}{\partial x}
\right\|.
$$

若某个样本已经接近局部驻点，则应有：

$$
\|g_i\|\rightarrow0.
$$

若仍远离局部最优区域，则梯度通常保持较大。

#### 数据比较

Theta-space：

$$
\|g^\Theta_{\text{test}}\|_{\text{mean}}=64.43
$$

$$
\|g^\Theta_{\text{train}}\|_{\text{mean}}=14.34
$$

测试集约为训练集的 4.5 倍。

Final Head：

$$
\|g^{\text{head}}_{\text{test}}\|_{\text{mean}}=482.01
$$

$$
\|g^{\text{head}}_{\text{train}}\|_{\text{mean}}=144.48
$$

测试集约为训练集的 3.3 倍。

#### 分析

这一结果与此前直接优化实验高度一致：

- 训练集网络输出已经位于相对较好的容量区域；
- 测试集网络输出仍远离适合自身的局部高容量区域。

因此可以进一步确认：

$$
\boxed{
\text{模型对训练信道形成了明显适配，但这种适配没有有效泛化到测试信道}
}
$$

---

### 4. 当前结论

本实验表明，Theta-space 中不同信道确实存在明显的样本依赖优化方向，且大量样本对具有强正向或强负向关系。

因此：

$$
\boxed{
\text{任务本身具有较强的 sample-dependent optimization 特性}
}
$$

但这些冲突经过网络特征和 Final Head 映射后，主要变成了近似正交的高维参数梯度，而非直接的强反向冲突。

因此目前不能简单认定：

> 样本间梯度冲突是导致低方差输出的主要原因。

更准确的结论是：

> Theta-space 中存在显著的样本依赖和方向冲突，但 Final Head 已经在一定程度上利用不同隐藏特征将这些冲突分离到不同参数方向。

此外，训练集和测试集之间显著的梯度范数差异进一步说明：

$$
\boxed{
\text{当前更明确的问题仍然是训练样本适配较好，但测试样本无法落入对应的局部高容量区域}
}
$$

---

### 5. 下一步测试

下一步应进一步计算每个样本梯度与真实 Batch 平均梯度之间的关系。

定义 Batch 平均梯度：

$$
\bar g=
\frac1N\sum_i g_i.
$$

对第 \(i\) 个样本，若执行一次梯度下降：

$$
\psi'=\psi-\eta\bar g,
$$

其一阶 loss 变化近似为：

$$
\Delta L_i
\approx
-\eta g_i^T\bar g.
$$

因此：

- 若

$$
g_i^T\bar g>0,
$$

Batch 更新会改善该样本；

- 若

$$
g_i^T\bar g<0,
$$

Batch 更新会使该样本变差。

下一步重点统计：

$$
P(g_i^T\bar g<0)
$$

以及：

$$
\cos(g_i,\bar g).
$$

该指标可以直接判断实际 SGD Batch 更新是否在牺牲部分样本来改善另一些样本，比单纯的 pairwise cosine 和抵消率 \(R\) 更能反映真实训练过程中的梯度冲突。
