## 1. Loss函数（目前仅启用L_cap），使用的是基于对角线计算的信道容量。
```
# losses/capacity.py
# ============================================================
# Module 3: 信道容量计算与组合损失函数  (CapacityLoss)
# ============================================================
# 功能: 给定预编码矩阵 W、信道 H、参数 Θ、SNR，计算可导的组合 Loss
#
# 组合公式:
#   L = L_cap + λ1·L_v_reg + λ2·L_φ_reg + λ3·L_repel
#
# ┌────────────┬──────────────────────────────────────────────────────────┐
# │ 损失分量    │ 物理含义                                               │
# ├────────────┼──────────────────────────────────────────────────────────┤
# │ L_cap      │ 负信道容量 = -C = Σ log2(E_ii)                        │
# │            │ 最小化 L_cap ⇔ 最大化容量 C                            │
# │            │ L_cap ≤ 0 (因为 E_ii ∈ (0,1])                         │
# ├────────────┼──────────────────────────────────────────────────────────┤
# │ L_v_reg    │ 波束正交性正则化                                        │
# │            │ sin²(π(θ_h1-θ_h2)·N1) + sin²(π(θ_v1-θ_v2)·N2)        │
# │            │ 零点: Δθ·N ∈ Z (DFT 网格上, 含退化零点 Δθ=0)           │
# ├────────────┼──────────────────────────────────────────────────────────┤
# │ L_φ_reg    │ 相位对齐正则化                                          │
# │            │ sin²(π·θ_φ1) + sin²(π·θ_φ2)                           │
# │            │ 零点: θ_φ ∈ Z (离散码本点 {0,1,2,3})                   │
# ├────────────┼──────────────────────────────────────────────────────────┤
# │ L_repel    │ 波束排斥力正则化                                        │
# │            │ relu(1 - |Δθ_h|·N1)² + relu(1 - |Δθ_v|·N2)²          │
# │            │ 当两波束间距 < 1 个 DFT 网格时产生二次排斥力            │
# │            │ 间距 ≥ 1 网格后惩罚立刻归零                             │
# │            │ 专治 Δθ=0 退化零点导致的模式坍塌                        │
# └────────────┴──────────────────────────────────────────────────────────┘
#
# L_v_reg vs L_repel 互补关系:
#   L_v_reg 的零点集 = {Δθ·N ∈ Z}, 包含正交 (Δθ=k/N, k≥1) 与退化 (Δθ=0)
#   L_repel 在退化零点 Δθ=0 处取最大值 1, 在正交零点 Δθ=1/N 处取 0
#   → 两者叠加后: Δθ=0 变为惩罚峰, Δθ=1/N 仍为零点 → 消除退化吸引盆
#
# MSE 协方差矩阵:
#   E_b = (I_υ + SNR · W^H · H_b^H · H_b · W)^{-1}
#
# 数值稳定性:
#   1. 矩阵求逆前加 eps·I 防病态 → 梯度 NaN
#   2. log2 运算前加 eps 防止 log(0)
#   3. SNR 支持外部传入, 也支持默认 config SNR
# ============================================================

import torch
import torch.nn as nn
from utils.config import LossConfig, AntennaConfig


class CapacityLoss(nn.Module):
    """组合容量损失函数

    全部计算基于 PyTorch 张量，支持自动微分反向传播。

    Args:
        cfg:     LossConfig (lambda1, lambda2, lambda3, eps)
        antenna: AntennaConfig (N1, N2, Nt, Nr, N_RB, υ, K, SNR_dB_High)
    """

    def __init__(self, cfg: LossConfig, antenna: AntennaConfig) -> None:
        super().__init__()
        self.lambda1 = cfg.lambda1
        self.lambda2 = cfg.lambda2
        self.lambda3 = cfg.lambda3
        self.eps = cfg.eps
        self.N1 = antenna.N1
        self.N2 = antenna.N2
        self.Nt = antenna.Nt
        self.Nr = antenna.Nr
        self.N_RB = antenna.N_RB
        self.upsilon = antenna.upsilon
        self.K = antenna.K
        self.SNR_dB_High = antenna.SNR_dB_High

    @staticmethod
    def _to_complex(H_real: torch.Tensor) -> torch.Tensor:
        """实数张量 → 复数张量

        H_real 末轴 = [实部, 虚部]，view_as_complex 将其合并为 complex64
        """
        return torch.view_as_complex(H_real)

    def mse_covariance(
        self,
        W: torch.Tensor,
        H_c: torch.Tensor,
        snr_linear: torch.Tensor,
    ) -> torch.Tensor:
        """计算 MSE 协方差矩阵 E_b

        E_b = (I_υ + SNR · W^H · H_b^H · H_b · W)^{-1}

        Args:
            W:          (B, Nt, υ)  complex64 预编码矩阵
            H_c:        (B, Nt, N_RB, Nr)  complex64 信道矩阵
            snr_linear: (B, 1)  线性 SNR 值

        Returns:
            E: (B, N_RB, υ, υ) complex64 MSE 协方差矩阵
        """
        B = W.shape[0]
        W_H = W.conj().transpose(-2, -1)

        H = H_c.permute(0, 2, 1, 3)
        HbHb = torch.matmul(H, H.conj().transpose(-2, -1))

        W_H_exp = W_H.unsqueeze(1).expand(-1, self.N_RB, -1, -1)
        W_exp = W.unsqueeze(1).expand(-1, self.N_RB, -1, -1)

        WH_HbHb = torch.matmul(W_H_exp, HbHb)
        WH_HbHb_W = torch.matmul(WH_HbHb, W_exp)

        I_u = torch.eye(
            self.upsilon, dtype=W.dtype, device=W.device
        ).unsqueeze(0).unsqueeze(0).expand(B, self.N_RB, -1, -1)

        snr_view = snr_linear.view(B, 1, 1, 1)
        A = I_u + snr_view * WH_HbHb_W

        E = torch.linalg.inv(A + self.eps * I_u)
        return E

    def capacity_loss(self, E: torch.Tensor) -> torch.Tensor:
        """容量损失 L_cap = Σ_b Σ_i log2(E_b[i,i])

        L_cap ≤ 0, 信道容量 C = -L_cap ≥ 0

        Args:
            E: (B, N_RB, υ, υ) MSE 协方差矩阵

        Returns:
            L_cap: (B,) 每个样本的负容量
        """
        diag = torch.real(torch.diagonal(E, dim1=-2, dim2=-1))
        log2_diag = torch.log2(diag + self.eps)
        L_cap = log2_diag.sum(dim=-1).sum(dim=-1)
        return L_cap

    def v_reg(self, theta: torch.Tensor) -> torch.Tensor:
        """波束正交性正则化 L_v_reg

        L_v_reg = sin²(π·(θ_h1-θ_h2)·N1) + sin²(π·(θ_v1-θ_v2)·N2)

        零点: (θ_h1-θ_h2)·N1 ∈ Z → 两波束落在 DFT 网格点 (正交或退化)
        极大值: (θ_h1-θ_h2)·N1 ∈ Z+0.5 → 两波束在相邻网格正中间

        注意: Δθ=0 也是零点 (退化), 需 L_repel 修正

        Args:
            theta: (B, 6) [θ_h1, θ_v1, θ_φ1, θ_h2, θ_v2, θ_φ2]

        Returns:
            L_v: (B,) 波束正交性惩罚
        """
        dh = theta[:, 0] - theta[:, 3]
        dv = theta[:, 1] - theta[:, 4]
        loss = torch.sin(torch.pi * dh * self.N1) ** 2
        loss = loss + torch.sin(torch.pi * dv * self.N2) ** 2
        return loss

    def repel_reg(self, theta: torch.Tensor) -> torch.Tensor:
        """波束排斥力正则化 L_repel

        L_repel = relu(1 - |Δθ_h|·N1)² + relu(1 - |Δθ_v|·N2)²

        物理含义:
          θ_h ∈ [0,1) 对应 N1 个 DFT 网格, 一个网格间距 = 1/N1
          当两波束角距 |Δθ_h| < 1/N1 时, relu 内 > 0, 产生排斥力
          当 |Δθ_h| ≥ 1/N1 时, relu = 0, 惩罚立刻归零, 不影响正常训练

        梯度行为 (以 θ_h 为例, 0 < Δθ < 1/N1):
          dL/dΔθ = -2·sign(Δθ)·N1·(1 - |Δθ|·N1)
          → Δθ > 0: 梯度 < 0, 推 Δθ 增大 (推开)
          → Δθ < 0: 梯度 > 0, 推 Δθ 减小 (推开)
          → 恒为排斥力, 方向始终远离 0

        与 L_v_reg 互补:
          L_v_reg 在 Δθ=0 取 0 (无排斥), L_repel 在 Δθ=0 取 1 (最强排斥)
          L_v_reg 在 Δθ=1/N 取 0 (正交目标), L_repel 在 Δθ=1/N 取 0 (不干扰)
          → 叠加后消除 Δθ=0 退化吸引盆, 保留 Δθ=k/N 正交零点

        圆周距离: min(|Δθ|, 1-|Δθ|), 处理 θ_h 在 0/1 边界附近环绕

        Args:
            theta: (B, 6) [θ_h1, θ_v1, θ_φ1, θ_h2, θ_v2, θ_φ2]

        Returns:
            L_repel: (B,) 排斥力惩罚 (≥ 0)
        """
        dh = (theta[:, 0] - theta[:, 3]).abs()
        dh = torch.min(dh, 1.0 - dh)

        dv = (theta[:, 1] - theta[:, 4]).abs()
        dv = torch.min(dv, 1.0 - dv)

        rh = torch.relu(1.0 - dh * self.N1) ** 2
        rv = torch.relu(1.0 - dv * self.N2) ** 2

        return rh + rv

    def phi_reg(self, theta: torch.Tensor) -> torch.Tensor:
        """相位对齐正则化 L_φ_reg

        L_φ_reg = sin²(π·θ_φ1) + sin²(π·θ_φ2)

        零点: θ_φ ∈ Z → 离散码本点 {0, 1, 2, 3}
        极大值: θ_φ ∈ Z+0.5 → 离离散点最远

        Args:
            theta: (B, 6)

        Returns:
            L_phi: (B,) 相位对齐惩罚
        """
        loss = torch.sin(torch.pi * theta[:, 2]) ** 2
        loss = loss + torch.sin(torch.pi * theta[:, 5]) ** 2
        return loss

    def forward(
        self,
        W: torch.Tensor,
        H_real: torch.Tensor,
        theta: torch.Tensor,
        snr: torch.Tensor | None = None,
        return_components: bool = False,
    ):
        """组合损失前向传播

        L = L_cap + λ1·L_v_reg + λ2·L_φ_reg + λ3·L_repel, 取 batch 均值返回标量

        Args:
            W:      (B, Nt, υ)  complex64 预编码矩阵
            H_real: (B, Nt, N_RB, Nr, 2)  原始实数信道
            theta:  (B, 6)  连续预编码参数
            snr:    (B, 1)  线性 SNR 值, 可选
            return_components: 若 True, 额外返回各分项均值 dict

        Returns:
            L: 标量 — batch 均值组合损失
            detail (仅 return_components=True): dict
                "L_cap":   容量损失均值 (≤0)
                "L_v":     波束正交性加权均值 (≥0)
                "L_phi":   相位对齐加权均值 (≥0)
                "L_repel": 排斥力加权均值 (≥0)
                "L_total": 总损失均值
        """
        if snr is None:
            snr_val = 10.0 ** (self.SNR_dB_High / 10.0)
            snr = snr_val * torch.ones(
                W.shape[0], 1, dtype=W.real.dtype, device=W.device
            )
        elif snr.dim() == 1:
            snr = snr.unsqueeze(-1)

        H_c = self._to_complex(H_real)
        E = self.mse_covariance(W, H_c, snr)
        L_cap = self.capacity_loss(E)
        L_v = self.v_reg(theta)
        L_phi = self.phi_reg(theta)
        L_repel = self.repel_reg(theta)

        L = L_cap + self.lambda1 * L_v + self.lambda2 * L_phi + self.lambda3 * L_repel
        L_total = L.mean()

        if return_components:
            detail = {
                "L_cap":   L_cap.mean().item(),
                "L_v":     (self.lambda1 * L_v).mean().item(),
                "L_phi":   (self.lambda2 * L_phi).mean().item(),
                "L_repel": (self.lambda3 * L_repel).mean().item(),
                "L_total": L_total.item(),
            }
            return L_total, detail

        return L_total
```

## 2. 
- 当前[CLS]保留，也有备份flatten版本，二者目前训练结果基本一致
- 波束域变换当前开启，但开启和关闭的训练结果基本一致
- 逐样本归一化：开启
- 是的，仅保留L_cap
- 保留归一化
- 保留layernorm
- 其他相关参数：
# configs/default.yaml
# ============================================================
# Search-Free PMI Selection — 生产配置
# ============================================================
# 各节对应 utils/config.py 中的 dataclass:
#   antenna → AntennaConfig, model → ModelConfig,
#   loss → LossConfig, train → TrainConfig,
#   test → TestConfig, debug → DebugConfig
# ============================================================

# ---- 天线物理参数 (AntennaConfig) ----
antenna:
  N1: 8                    # 水平天线振子数 (单极化)
  N2: 2                    # 垂直天线振子数 (单极化)
  Nt: 32                   # 总发射天线数 (= 2×N1×N2, 双极化)
  Nr: 4                    # 接收天线数 (2×双极化)
  N_RB: 32                 # 频域子载波数 (粗粒度分组)
  O1: 4                    # 水平过采样因子 (仅量化用)
  O2: 4                    # 垂直过采样因子 (仅量化用)
  upsilon: 2               # 传输流数 (RI)
  K: 2                     # 独立波束组数 (upsilon≤4 时 K=upsilon)
  P_CSI_RS: 1.0            # 功率归一化因子
  SNR_dB_Low: 20.0         # 训练 SNR 下界 (dB)
  SNR_dB_High: 20.0        # 训练 SNR 上界 / 评估用固定 SNR (dB)

# ---- 模型架构 (ModelConfig) ----
model:
  D: 128                   # Token 特征维度 (d_model)
  n_heads: 4               # 多头注意力头数
  n_layers: 2              # Transformer 编码器层数
  ffn_ratio: 4             # FFN 隐层扩展比 (FFN_dim = D × ffn_ratio = 512)
  dropout: 0.1             # 残差 / FFN Dropout 率
  attn_dropout: 0.1        # 注意力权重 Dropout 率
  head_name: "ri2"         # 输出头注册名 (ri2=双流统一MLP, ri2_iso=双流隔离MLP)
  film_hidden_dim: 64      # FiLM 生成器隐层维度
  head_intermediate_dim: 512  # 输出头 MLP 中间层维度

# ---- 损失函数 (LossConfig) ----
# L_total = L_cap + λ1·L_v_reg + λ2·L_φ_reg + λ3·L_repel
loss:
  lambda1: 0               # L_v_reg 权重 (波束正交性)
  lambda2: 0               # L_φ_reg 权重 (相位对齐)
  lambda3: 0               # L_repel 权重 (波束排斥力, 防模式坍缩)
  eps: 1.0e-06             # 数值稳定抖动 (矩阵求逆 / log2 前)

# ---- 训练流程 (TrainConfig) ----
train:
  device: "cuda:3"         # 训练设备 (cuda:N / cpu / null=自动检测)
  num_train_samples: 5000  # 训练集样本数上限 (null=全部)
  num_val_samples: 2000    # 验证集样本数上限 (null=全部)
  lr: 0.001                 # AdamW 初始学习率 (峰值)
  weight_decay: 0.0001       # AdamW 权重衰减
  batch_size: 1024         # 批大小
  epochs: 1000              # 最大训练轮数 (受早停约束)
  warmup_ratio: 0.01        # LR 预热步数占比 (前 x% 线性升)
  max_grad_norm: 999       # 梯度裁剪全局 L2 范数上界
  patience: 999             # 早停耐心值 (连续 N epoch 无改善则停)

# ---- 调试探针 (DebugConfig) ----
# debug 为总开关: false 时跳过所有探针, true 时按子开关控制
debug:
  debug: true              # [总开关] 启用调试探针
  debug_grad_norm: false   # [探针] 输出头最终权重梯度范数 (每epoch, 诊断梯度锁死)
  debug_head_isolation: false  # [探针] 输出头物理隔离 (两条独立MLP分支, 诊断拔河效应)
  debug_encoded_analysis: false  # [探针] Backbone 特征各向异性坍缩诊断 (Mean_Cos_Sim + Top1_SVD_Ratio)
  debug_verbose: true     # [输出] 配置摘要 / 容量上界 / 数据集规模 / 训练启动信息
  debug_diag: true         # [输出] 逐epoch Diag行 (θ_circ_std, beam_corr) + 评估诊断段 (Θ分布, 随机基线)
  debug_history: true     # [输出] 训练历史全文打印 (epoch 逐行)
  beam_transform: false   # [预处理] 是否启用正交2D-DFT波束域变换 (天线域→波束域)

# ---- 推理评估 (TestConfig) ----
test:
  device: "cuda:3"         # 评估设备
  num_test_samples: 1000   # 测试集样本数上限 (null=全部)
  batch_size: 64           # 评估批大小


## 3. 训练结果如下：

  Train: L_total -811.7589  L_cap -811.7589  L_v 0.0000  L_phi 0.0000  L_repel 0.0000
  Val:   L_total -765.9209  Cap 765.92  L_cap -765.9209  L_v 0.0000  L_phi 0.0000  L_repel 0.0000  LR 0.00e+00
  Diag:  θ_circ_std=[0.0437, 0.1599, 0.6338, 0.0458, 0.1515, 0.5845]  beam_corr=0.0230  Cap_det=781.74  Cap_stream=[382.74, 383.18]


============================================================
推理评估结果 - 验证集 (2,000 样本)
============================================================

--- 连续推理 (Continuous) ---
  C = 765.92 bps/Hz
  每RB每流: stream1=5.98, stream2=5.99

--- 量化推理 (Quantized) ---
  C = 766.14 bps/Hz

--- 量化损失 ---
  gap = -0.22 bps/Hz (-0.0%)

--- 随机基线 (Random Codebook) ---
  C = 762.79 bps/Hz
  beam_corr = 0.0866

--- 波束诊断 (模型) ---
  beam_corr = 0.0230 (1.0=完全相同, 0.0=正交)
  L_v_reg   = 0.9827
  L_φ_reg   = 1.0134

--- Θ 分布 (连续, 圆周统计) ---
    θ_h1: circ_mean=0.2415, circ_std=0.0437
    θ_v1: circ_mean=0.3917, circ_std=0.1599
    θ_φ1: circ_mean=1.5049, circ_std=0.6338
    θ_h2: circ_mean=0.7781, circ_std=0.0458
    θ_v2: circ_mean=0.8105, circ_std=0.1515
    θ_φ2: circ_mean=2.7222, circ_std=0.5845

--- Θ_q 分布 (量化, 圆周统计) ---
    θ_h1: circ_std=0.2419
    θ_v1: circ_std=0.3916
    θ_φ1: circ_std=1.5133
    θ_h2: circ_std=0.7782
    θ_v2: circ_std=0.8175
    θ_φ2: circ_std=2.7271

============================================================


## 4. 没有做逐样本最优基线，但是绘制了信道功率方向图，不同信道样本主径方向有较大不同。

## 5. 指的应该是实验结果中的随机基线 (Random Codebook)，随机选择两条正交的波束，看信道容量。实验记录中的此部分分析可以忽略

## 6. 是的，每个样本除以自己的功率

## 7. Loss计算应该用原始H，你发现了我代码中的bug，目前正在修改。CodeBook输出的W应该是和项目设计文档中的计算方法一致的

## 8. 训练时SNR均为20dB，计划在单SNR情况下取得收敛，再考虑SNR泛化。

## 9. 指的是波束域的位置，横纵坐标分别为方位角和天顶角，与信道功率方向图所叠加。




