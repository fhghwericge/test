# ============================================================
# 步骤 5: Test 1 - 单样本直接优化 Theta (诊断 Basin Locking)
# 抛弃神经网络，针对单个信道样本直接对连续参数 Theta 做梯度下降
# 观察纯数学优化是否能找到比神经网络高得多的信道容量
# ============================================================
import copy

# ---- 实验参数设置 ----
num_test_samples = 4        # 抽取几个样本进行测试
learning_rate = 0.05        # 直接优化的学习率
num_steps = 300             # 每个样本的梯度下降步数
num_restarts = 3            # 每个样本进行几次随机初始化 (Random Restarts)

# 为了纯粹评估容量，我们临时克隆一个 cap_loss 并去掉正则化项 (只看 L_cap)
cap_loss_pure = copy.deepcopy(cap_loss)
cap_loss_pure.lambda1 = 0.0
cap_loss_pure.lambda2 = 0.0
# cap_loss_pure.lambda3 = 0.0 # 如果你的代码里有 L_repel

print(f"开始执行 Test 1 (直接优化 Theta)... 共测试 {num_test_samples} 个样本\n")

for i in range(num_test_samples):
    # print(f"{"="*20} 样本 {i} {"="*20}")
    
    # 1. 提取单个信道样本，并增加 Batch 维度使其 shape 为 (1, ...)
    H_single = test_ds[i][0].unsqueeze(0).to(device)
    snr_single = test_ds[i][1].unsqueeze(0).to(device)
    
    # 2. 获取神经网络的基准表现 (Baseline)
    model.eval()
    with torch.no_grad():
        theta_net = model(H_single, snr=snr_single)
        W_net = codebook(theta_net)
        # 用无正则化的 loss 计算纯容量
        loss_net = cap_loss_pure(W_net, H_single, theta_net, snr_single)
        cap_net = -loss_net.item()
    
    print(f"[网络 Baseline] 容量: {cap_net:.2f} bits | Theta: {theta_net.cpu().numpy()[0].round(3)}")
    
    best_cap_overall = -float('inf')
    best_theta_overall = None
    
    # 3. 多起点直接优化 (Random Restarts)
    for restart in range(num_restarts):
        # 初始化策略：第 0 次可以尝试从网络输出开始，后面几次用纯随机初始化
        if restart == 0:
            print(f"\n  --- Restart 0: 以[神经网络输出]为起点 ---")
            theta_init = theta_net.detach().clone()
        else:
            print(f"\n  --- Restart {restart}: 以[纯随机]为起点 ---")
            theta_init = torch.rand(1, 6, device=device)
            theta_init[:, [2, 5]] *= 4.0 # 极化相位 phi 的取值范围是 0~4
        
        # 【核心逻辑】将 Theta 声明为可优化参数！
        Theta_n = torch.nn.Parameter(theta_init)
        
        # 优化器只负责优化这个具体的 Theta_n
        optimizer = torch.optim.Adam([Theta_n], lr=learning_rate)
        
        best_cap_local = -float('inf')
        
        for step in range(num_steps):
            optimizer.zero_grad()
            
            # 构建预编码矩阵
            W_direct = codebook(Theta_n)
            
            # 计算纯容量损失
            loss_direct = cap_loss_pure(W_direct, H_single, Theta_n, snr_single)
            
            # 反向传播，更新 Theta_n
            loss_direct.backward()
            optimizer.step()
            
            # 【物理约束】由于抛弃了网络里的 Sigmoid 映射，我们必须手动把参数截断在合法范围内
            with torch.no_grad():
                Theta_n[:, [0, 1, 3, 4]] = torch.clamp(Theta_n[:, [0, 1, 3, 4]], 0.0, 1.0)
                Theta_n[:, [2, 5]] = torch.clamp(Theta_n[:, [2, 5]], 0.0, 4.0)
            
            current_cap = -loss_direct.item()
            
            # 记录当前 restart 的最优值
            if current_cap > best_cap_local:
                best_cap_local = current_cap
                
            # 记录所有 restart 的全局最优值
            if current_cap > best_cap_overall:
                best_cap_overall = current_cap
                best_theta_overall = Theta_n.detach().clone()
                
            if (step + 1) == num_steps:
                print(f"      Step {step+1}/{num_steps} 收敛容量: {best_cap_local:.2f} bits")
                
    # 4. 样本总结对比
    print(f"\n>>> 样本 {i} 总结 <<<")
    print(f"    网络输出容量 : {cap_net:.2f}")
    print(f"    直接优化容量 : {best_cap_overall:.2f}")
    diff = best_cap_overall - cap_net
    print(f"    容量提升差距 : {diff:+.2f} bits")
    print(f"    最优 Theta   : {best_theta_overall.cpu().numpy()[0].round(3)}\n")















-----------------------测试集---------------------------

    开始执行 Test 1 (直接优化 Theta)... 共测试 4 个样本

[网络 Baseline] 容量: 768.03 bits | Theta: [0.293 0.763 1.474 0.966 0.439 0.836]

  --- Restart 0: 以[神经网络输出]为起点 ---
      Step 300/300 收敛容量: 810.75 bits

  --- Restart 1: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 798.36 bits

  --- Restart 2: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 802.42 bits

>>> 样本 0 总结 <<<
    网络输出容量 : 768.03
    直接优化容量 : 810.75
    容量提升差距 : +42.72 bits
    最优 Theta   : [0.352 1.    0.839 0.862 0.879 2.304]

[网络 Baseline] 容量: 785.23 bits | Theta: [0.291 0.919 1.453 0.946 0.405 1.288]

  --- Restart 0: 以[神经网络输出]为起点 ---
      Step 300/300 收敛容量: 828.18 bits

  --- Restart 1: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 814.42 bits

  --- Restart 2: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 817.16 bits

>>> 样本 1 总结 <<<
    网络输出容量 : 785.23
    直接优化容量 : 828.18
    容量提升差距 : +42.95 bits
    最优 Theta   : [0.107 1.    2.667 0.896 0.67  2.162]

[网络 Baseline] 容量: 786.38 bits | Theta: [0.317 0.867 0.096 0.999 0.232 1.357]

  --- Restart 0: 以[神经网络输出]为起点 ---
      Step 300/300 收敛容量: 804.41 bits

  --- Restart 1: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 837.36 bits

  --- Restart 2: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 837.41 bits

>>> 样本 2 总结 <<<
    网络输出容量 : 786.38
    直接优化容量 : 837.41
    容量提升差距 : +51.03 bits
    最优 Theta   : [0.276 0.803 3.936 0.198 0.339 0.648]

[网络 Baseline] 容量: 764.31 bits | Theta: [2.900e-01 9.920e-01 1.000e-03 1.000e+00 3.730e-01 2.261e+00]

  --- Restart 0: 以[神经网络输出]为起点 ---
      Step 300/300 收敛容量: 819.97 bits

  --- Restart 1: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 812.11 bits

  --- Restart 2: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 823.77 bits

>>> 样本 3 总结 <<<
    网络输出容量 : 764.31
    直接优化容量 : 823.77
    容量提升差距 : +59.46 bits
    最优 Theta   : [0.29  0.178 2.732 0.43  0.908 0.785]











-----------------------训练集---------------------------


开始执行 Test 1 (直接优化 Theta)... 共测试 4 个样本

[网络 Baseline] 容量: 822.73 bits | Theta: [0.213 0.516 0.688 0.954 0.965 3.044]

  --- Restart 0: 以[神经网络输出]为起点 ---
      Step 300/300 收敛容量: 822.84 bits

  --- Restart 1: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 824.08 bits

  --- Restart 2: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 797.83 bits

>>> 样本 0 总结 <<<
    网络输出容量 : 822.73
    直接优化容量 : 824.08
    容量提升差距 : +1.35 bits
    最优 Theta   : [0.267 0.535 2.821 0.219 0.566 0.809]

[网络 Baseline] 容量: 822.01 bits | Theta: [0.336 0.755 0.866 0.904 0.32  0.   ]

  --- Restart 0: 以[神经网络输出]为起点 ---
      Step 300/300 收敛容量: 822.04 bits

  --- Restart 1: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 822.04 bits

  --- Restart 2: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 808.04 bits

>>> 样本 1 总结 <<<
    网络输出容量 : 822.01
    直接优化容量 : 822.04
    容量提升差距 : +0.03 bits
    最优 Theta   : [0.334 0.753 0.876 0.906 0.321 0.   ]

[网络 Baseline] 容量: 811.99 bits | Theta: [0.277 0.69  0.    1.    0.748 2.256]

  --- Restart 0: 以[神经网络输出]为起点 ---
      Step 300/300 收敛容量: 812.00 bits

  --- Restart 1: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 822.15 bits

  --- Restart 2: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 799.90 bits

>>> 样本 2 总结 <<<
    网络输出容量 : 811.99
    直接优化容量 : 822.15
    容量提升差距 : +10.16 bits
    最优 Theta   : [1.    0.765 2.472 0.29  0.463 2.664]

[网络 Baseline] 容量: 819.52 bits | Theta: [0.304 0.941 1.976 0.973 0.735 2.264]

  --- Restart 0: 以[神经网络输出]为起点 ---
      Step 300/300 收敛容量: 819.55 bits

  --- Restart 1: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 817.58 bits

  --- Restart 2: 以[纯随机]为起点 ---
      Step 300/300 收敛容量: 827.39 bits

>>> 样本 3 总结 <<<
    网络输出容量 : 819.52
    直接优化容量 : 827.39
    容量提升差距 : +7.87 bits
    最优 Theta   : [0.646 0.49  2.634 0.495 0.685 2.015]
