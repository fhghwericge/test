## Test 4：样本间梯度冲突诊断

### 实验目的
针对训练完成后的模型，分别在训练集与测试集抽取 64 个样本，逐样本反向传播，分析不同样本之间的梯度方向关系，判断共享网络参数是否存在明显的梯度冲突。

分别考察两类梯度：

1. **Theta-space 梯度**
$$
g_i^{\Theta}=\frac{\partial L_i}{\partial \Theta_i}
$$
表示在不考虑共享神经网络参数的情况下，第 $i$ 个样本希望其预编码参数 $\Theta_i$ 朝哪个方向更新。

2. **Final Head 梯度**
$$
g_i^{\mathrm{head}}=\nabla_{\psi_{\mathrm{head}}}L_i
$$
表示经过网络映射后，第 $i$ 个样本对最终输出层共享参数的更新要求。

两者满足链式关系：
$$
g_i^{\mathrm{head}}=J_i^Tg_i^{\Theta}
$$
其中
$$
J_i=\frac{\partial \Theta_i}{\partial \psi_{\mathrm{head}}}.
$$

梯度抵消率定义为：
$$
R=
\frac{
\left\|
\frac{1}{N}\sum_i g_i
\right\|
}{
\frac{1}{N}\sum_i\|g_i\|
}.
$$
$R$ 越小，说明平均后的有效梯度相对于单样本梯度越弱。但需要注意，较小的 $R$ 既可能来自反向冲突，也可能来自高维梯度之间的近似正交，因此需结合余弦相似度共同判断。

样本间梯度余弦相似度定义为：
$$
\cos(g_i,g_j)=
\frac{
g_i^Tg_j
}{
\|g_i\|\|g_j\|
}.
$$

### 1. Theta-space 梯度

#### 原理
Theta-space 梯度直接描述每个信道样本在预编码参数空间中的局部优化方向：
$$
g_i^{\Theta}
=
\frac{\partial L_i}{\partial\Theta_i}.
$$
若两个样本满足
$$
\cos(g_i^{\Theta},g_j^{\Theta})<0,
$$
说明二者希望 $\Theta$ 朝相反方向更新；若余弦接近 0，则两者优化方向近似正交。

#### 数据（比较）

| 指标 | 训练集 | 测试集 |
|---|---:|---:|
| Mean Grad Norm | 14.34 | 64.43 |
| $\|\mathrm{mean}(g)\|$ | 3.27 | 11.79 |
| Cancellation Ratio $R$ | 0.2282 | 0.1830 |
| Mean Cosine | 0.0318 | -0.0008 |
| Median Cosine | 0.0429 | -0.0042 |
| Cosine $<0$ | 47.77% | 50.10% |
| Cosine $<-0.5$ | 20.49% | 26.93% |
| Cosine $>0.5$ | 24.01% | 27.48% |

测试集平均梯度范数约为训练集的：
$$
\frac{64.43}{14.34}\approx4.5.
$$

#### 分析
测试集的 Theta-space 梯度明显大于训练集，说明测试样本的网络输出仍远离其对应容量驻点，而训练样本已经被模型较充分地优化。这与此前单样本直接优化结果一致：训练样本继续优化的收益较小，而测试样本仍可获得数十 bits 的容量提升。

测试集的平均余弦相似度接近 0，但不能解释为“没有结构”。其中约 26.93% 的样本对满足：
$$
\cos(g_i^{\Theta},g_j^{\Theta})<-0.5,
$$
同时约 27.48% 满足：
$$
\cos(g_i^{\Theta},g_j^{\Theta})>0.5.
$$
说明不同信道之间同时存在明显的同向与反向优化需求，正负方向在统计平均后相互抵消，使 Mean Cosine 接近 0。

因此 Theta-space 的结果表明：
$$
\boxed{
\text{不同信道确实具有明显不同、甚至相反的局部参数优化方向}
}
$$
这与不同信道具有不同最优 beam、容量 landscape 多峰的现象一致。

训练集仍存在较明显的方向分化，但梯度范数显著下降，且强负相关比例由 26.93% 降至 20.49%，说明训练过程已经将训练样本移动到一片梯度更弱、剩余优化需求相对更协调的区域。

### 2. Final Head 梯度

#### 原理
Final Head 梯度描述样本对共享输出层参数的实际更新需求。设最后一层为：
$$
r_i=Wz_i+b,
$$
其中 $z_i$ 为样本特征，$r_i$ 为 12 维 atan2 参数化前的 raw output。

定义：
$$
\delta_i=\frac{\partial L_i}{\partial r_i},
$$
则最后一层权重梯度为：
$$
\frac{\partial L_i}{\partial W}
=
\delta_i z_i^T.
$$
两个样本的梯度相似度同时受到输出梯度与输入特征相似度影响：
$$
\cos(g_i^W,g_j^W)
\approx
\cos(\delta_i,\delta_j)
\cos(z_i,z_j).
$$
因此即使两个样本在 Theta-space 中存在冲突，只要其隐藏特征 $z_i$ 与 $z_j$ 差异较大，映射到共享权重空间后的梯度仍可能近似正交。

#### 数据（比较）

| 指标 | 训练集 | 测试集 |
|---|---:|---:|
| Mean Grad Norm | 144.48 | 482.01 |
| $\|\mathrm{mean}(g)\|$ | 30.62 | 67.52 |
| Cancellation Ratio $R$ | 0.2119 | 0.1401 |
| Mean Cosine | 0.0099 | 0.0004 |
| Median Cosine | 0.0009 | -0.0002 |
| Cosine $<0$ | 48.81% | 50.00% |
| Cosine $<-0.5$ | 0.10% | 0.10% |
| Cosine $>0.5$ | 0.89% | 0.20% |

测试集 Final Head 平均梯度范数约为训练集的：
$$
\frac{482.01}{144.48}\approx3.3.
$$

#### 分析
与 Theta-space 类似，测试集 Final Head 梯度明显更大，进一步说明测试样本仍具有较强的参数修正需求。

但 Final Head 的梯度结构与 Theta-space 明显不同。虽然约 50% 的梯度对余弦为负，但真正强负相关：
$$
\cos(g_i,g_j)<-0.5
$$
仅占 0.10%，强正相关：
$$
\cos(g_i,g_j)>0.5
$$
也仅占 0.20%。

这说明 Final Head 的样本梯度主要集中在：
$$
\cos(g_i,g_j)\approx0
$$
即彼此近似正交，而不是大量直接反向冲突。

测试集 Final Head 的：
$$
R=0.1401
$$
与 64 个高维近似独立梯度的参考量级
$$
\frac{1}{\sqrt{64}}=0.125
$$
较接近。因此较低的 $R$ 更可能来源于高维梯度近似正交，而不能直接解释为严重的梯度抵消。

Theta-space 中约 26.93% 的强负相关，在进入 Final Head 后下降至 0.10%，说明网络隐藏特征实际上将不同样本的更新需求映射到了较为独立的参数方向。

因此：
$$
\boxed{
\text{Final Head 本身暂未表现出明显的强反向梯度冲突}
}
$$

### 3. Train/Test 梯度强度差异

#### 原理
梯度范数反映当前位置继续沿容量目标优化的局部驱动力：
$$
\left\|
\nabla_{\Theta}L
\right\|.
$$
若训练样本梯度较小、测试样本梯度较大，则说明模型对训练样本已经形成较强适配，但测试样本输出仍远离合适的容量驻点。

#### 数据（比较）

Theta-space：
$$
\|\nabla_{\Theta}L\|_{\mathrm{train}}
=14.34
$$
$$
\|\nabla_{\Theta}L\|_{\mathrm{test}}
=64.43.
$$

Final Head：
$$
\|\nabla_{\mathrm{head}}L\|_{\mathrm{train}}
=144.48
$$
$$
\|\nabla_{\mathrm{head}}L\|_{\mathrm{test}}
=482.01.
$$

#### 分析
测试集在 Theta-space 和 Final Head 中均表现出显著更大的梯度范数，与之前的单样本直接优化实验一致。

因此目前可以进一步确认：
$$
\boxed{
\text{模型已经明显适配训练样本，但这种局部优化能力未能有效泛化到测试样本}
}
$$

这也是当前训练集与测试集容量差距的重要局部微分证据。

### 4. 当前结论

当前实验支持以下结论：

1. **Theta-space 存在显著的样本相关优化方向差异。**  
   不同信道对 $\Theta$ 的局部更新要求存在大量同向和反向关系，说明任务本身具有较强的 sample-dependent optimization 特征。

2. **Final Head 并未表现出严重的直接反向梯度冲突。**  
   Theta-space 中的强冲突在经过网络特征映射后，大部分转化为高维参数空间中的近似正交梯度。

3. **测试集梯度明显大于训练集。**  
   说明训练样本已经被模型较充分地适配，而测试样本输出仍远离对应信道的容量驻点。

因此目前不能简单认为：
$$
\boxed{
\text{“Final Head 梯度冲突”是导致低方差输出的主要原因}
}
$$
更准确的判断是：
$$
\boxed{
\text{任务本身存在明显的样本相关优化方向差异，但网络参数空间能够一定程度地将这些差异解耦}
}
$$

### 5. 下一步测试

下一步应直接分析真实 Batch 更新方向对各样本的影响。

定义 Batch 平均梯度：
$$
\bar g=
\frac{1}{N}\sum_i g_i.
$$

对于样本 $i$，执行一次梯度下降后，其一阶 loss 变化近似为：
$$
\Delta L_i
\approx
-\eta g_i^T\bar g.
$$

若：
$$
g_i^T\bar g>0,
$$
则 Batch 更新有利于该样本；若：
$$
g_i^T\bar g<0,
$$
则 Batch 更新会在一阶近似下使该样本性能变差。

因此下一步建议统计：

- $g_i^T\bar g<0$ 的样本比例；
- $\cos(g_i,\bar g)$ 的均值与分布；
- Train/Test 的差异；
- Head、Backbone、Preprocessor 各层的 Batch-gradient alignment。

该指标可以直接判断：
$$
\boxed{
\text{真实 SGD Batch 更新是否在改善一部分样本的同时损害另一部分样本}
}
$$
