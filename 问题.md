import torch.nn.functional as F

# ============================================================
# 步骤 5: Test 4 - 样本间梯度冲突 (Gradient Conflict) 诊断
# 逐样本反向传播，提取最后一层权重的梯度，计算相似度和抵消率
# ============================================================
print("\n" + "="*50)
print("开始执行 Test 4: 样本间梯度冲突诊断")
print("="*50)

model.train() # 保持可导模式

# 我们选择观察输出头最后一层权重的梯度，这层直接决定了最终输出的波束 (x,y)
target_layer = model.head.mlp[2] 
per_sample_grads = []

# 逐样本独立计算梯度
for i in range(B):
    model.zero_grad() # 清空之前的梯度
    
    # 取出单样本并保持 batch 维度 (1, ...)
    H_i = H_real[i:i+1]
    snr_i = snr_test[i:i+1]
    
    # 前向传播
    theta_i = model(H_i, snr=snr_i)
    W_i = codebook(theta_i)
    
    # 计算该样本的 loss
    loss_i = cap_loss(W_i, H_i, theta_i, snr_i)
    
    # 反向传播 (只针对当前样本)
    loss_i.backward()
    
    # 提取最后一层权重的梯度，展平为 1 维向量
    grad_vector = target_layer.weight.grad.view(-1).clone()
    per_sample_grads.append(grad_vector)

# 将 B 个样本的梯度堆叠成矩阵，形状为 (B, 参数数量)
G_matrix = torch.stack(per_sample_grads)

# ------------------------------------------------------------
# 指标 1：梯度抵消率 R (Gradient Cancellation Ratio)
# 公式：||平均梯度|| / 平均(||各个梯度||)
# ------------------------------------------------------------
mean_grad = G_matrix.mean(dim=0)          # 算术平均梯度 (模拟 Batch 训练时的真实更新方向)
norm_of_mean = mean_grad.norm().item()    # 平均梯度的长度
mean_of_norms = G_matrix.norm(dim=1).mean().item() # 各个梯度长度的平均值

# R 值越接近 1，说明大家方向越一致；越接近 0，说明互相抵消越严重
R = norm_of_mean / (mean_of_norms + 1e-8)

print(f"指标 1: 梯度抵消率 (R-value)")
print(f"  - 平均梯度的范数 (||mean(g)||): {norm_of_mean:.4f}")
print(f"  - 梯度的平均范数 (mean(||g||)): {mean_of_norms:.4f}")
print(f"  -> R = {R:.4f}  (1.0=完全一致, 0.0=完全冲突)")

# ------------------------------------------------------------
# 指标 2：样本间梯度余弦相似度矩阵 (Cosine Similarity)
# ------------------------------------------------------------
# 归一化每个梯度向量
G_normed = F.normalize(G_matrix, p=2, dim=1)
# 计算两两之间的余弦相似度
cosine_sim_matrix = torch.mm(G_normed, G_normed.t())

# 打印完整的相似度矩阵 (B=4, 打印出来很清晰)
print(f"\n指标 2: 样本间梯度余弦相似度矩阵 (B={B})")
# 格式化打印矩阵
for row in cosine_sim_matrix:
    print("  [" + ", ".join([f"{val.item():>6.3f}" for val in row]) + "]")

# 计算非对角线元素的平均相似度
mask = torch.ones_like(cosine_sim_matrix) - torch.eye(B).to(device)
mean_cosine_sim = (cosine_sim_matrix * mask).sum() / (B * (B - 1))
print(f"  -> 平均余弦相似度 = {mean_cosine_sim:.4f}  (1.0=同向, 0.0=正交, -1.0=反向)")

model.eval() # 恢复推理模式
