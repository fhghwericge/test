## 当前问题描述

当前模型希望学习从信道到双流预编码参数的映射：

$$
H \rightarrow \Theta(H)
$$

其中不同信道理论上应对应不同的最优码本参数。

现有实验表明：

1. **不同信道的最优码本确实存在明显差异。**  
   穷举搜索得到的最优波束参数跨样本圆周标准差约为：

$$
[0.1866,\ 0.2305,\ 0.2240,\ 0.3020]
$$

说明最优 PMI 具有明显的样本依赖性。

2. **神经网络输出的参数方差明显偏小。**  
   无论训练集还是验证/测试集，模型输出均集中在较窄的参数区域，没有呈现与穷举最优解相当的样本差异。

3. **训练集和测试集表现存在明显不同。**

对训练样本，从网络输出开始继续直接优化参数，容量几乎不再提高：

$$
C_{\text{net,train}}\approx819.06
$$

$$
C_{\text{direct-from-net,train}}\approx819.11
$$

平均提升仅约：

$$
0.05\ \text{bits}
$$

说明训练样本的网络输出已经接近其当前连续容量面上的局部驻点。

而测试样本从网络输出继续优化后，容量可以显著提高：

$$
C_{\text{net,test}}\approx775.99
$$

$$
C_{\text{direct-from-net,test}}\approx815.83
$$

平均提升约：

$$
39.84\ \text{bits}
$$

说明测试样本的网络输出甚至没有落在合适的局部最优附近。

---

## 当前原因分析

### 1. 单纯的 Backbone 特征坍缩基本可以排除

此前已经确认：

- Transformer 中间特征方差正常；
- Token 表征不存在明显同质化；
- 有效特征秩正常；
- FiLM 数值爆炸和位置编码过强问题已经修复；
- 更换 Flatten Head、CLS Head、双流独立 Head 后现象基本不变。

因此目前更可能的问题不是：

> 不同信道无法被网络区分。

而是：

> 不同信道的特征虽然不同，但这些差异没有被正确转换成不同的波束决策。

---

### 2. 容量优化空间确实具有明显多峰性

单样本双流码本容量图显示：

- 存在多个相互分离的高容量峰；
- 不同峰之间存在明显容量谷；
- 双流交换存在对称性；
- 不同随机初始化可能收敛到明显不同的容量结果。

例如某些测试样本中：

$$
C_{\text{restart A}}\approx804
$$

而另一随机初始化可以达到：

$$
C_{\text{restart B}}\approx837
$$

说明不同初始化确实可能进入不同的局部 basin。

因此多峰性和 basin dependence 是真实存在的。

---

### 3. 但“网络被卡在一个共同局部最优”不能完全解释当前现象

训练集和测试集直接优化结果表明：

对于训练样本：

$$
\nabla_{\Theta} C(H_{\text{train}},\Theta_{\text{net}})
\approx 0
$$

网络已经将训练样本送到了某个局部最优附近。

但对于测试样本：

$$
\nabla_{\Theta} C(H_{\text{test}},\Theta_{\text{net}})
\neq 0
$$

从网络输出继续优化仍可获得几十 bits 的容量提升。

因此当前问题不能简单描述为：

> 所有样本都被锁进了同一个次优 basin。

更准确的描述是：

> 模型能够在训练样本上学习到某个局部最优区域，但没有学习到可以泛化到未见信道的 basin-selection / local-optimum mapping。

---

### 4. 当前最可能的问题是局部最优映射的泛化失败

训练过程中，模型实际上在学习：

$$
H \rightarrow \Theta_{\text{local}}(H)
$$

而不是直接执行离散全局搜索：

$$
H \rightarrow \Theta_{\text{global}}^{*}(H)
$$

对于训练样本，模型已经可以将输出调整到局部容量驻点附近，因此继续直接优化几乎没有收益。

但对于未见测试信道，模型给出的参数仍然停留在训练期间学到的狭窄参数区域，无法正确预测该信道对应的局部最优位置。

因此目前更符合实验结果的机制是：

$$
H_{\text{train}}
\rightarrow
\Theta_{\text{local}}(H_{\text{train}})
$$

但

$$
H_{\text{test}}
\rightarrow
\Theta_{\text{pred}}
\not\approx
\Theta_{\text{local}}(H_{\text{test}})
$$

这也可以同时解释：

- 训练集容量较高；
- 测试集容量明显降低；
- 训练集和测试集参数标准差都偏小；
- 直接优化测试样本可以获得大幅提升。

---

### 5. 输出参数化仍存在一个需要优先排除的因素：Sigmoid 饱和

神经网络实际输出为：

$$
\theta_{h,v}=\sigma(z)
$$

$$
\theta_{\phi}=4\sigma(z)
$$

因此梯度满足：

$$
\frac{\partial L}{\partial z}=\frac{\partial L}{\partial \theta}\theta(1-\theta)
$$

而当前模型输出中大量参数已经靠近：

$$
0
$$

或

$$
1
$$

此时：

$$
\theta(1-\theta)\rightarrow0
$$

可能导致参数空间中本来存在的有效容量梯度，在经过 Sigmoid 后被显著削弱。

目前的直接优化实验直接优化了参数本身，并使用 clamp，因此绕过了这一层梯度衰减。

因此这一因素需要在进一步分析共享网络问题之前单独排除。

---

## 当前阶段结论

目前最符合实验结果的解释为：

> 容量 landscape 具有明显的多峰结构，不同信道对应不同的局部/全局高容量区域。模型能够在训练样本上学习到局部容量最优附近的输出，但这种从信道到局部最优参数的映射没有有效泛化到未见信道，因此测试样本输出仍集中在训练期间形成的狭窄参数区域，并远离对应信道真正的局部最优。

因此当前主问题已经从：

> 模型是否能够优化容量？

转变为：

> 模型为什么能够在训练样本上找到局部最优，却无法学习一个能够泛化的样本依赖波束选择映射？

---

## 下一步测试方案

### Test 2：Direct-Logit Optimization

目的：判断 Sigmoid 参数化是否限制了梯度优化。

不再直接优化参数：

$$
\Theta
$$

而是优化网络输出之前的无约束变量：

$$
z
$$

使用与神经网络完全一致的映射：

$$
\theta_{h,v}=\sigma(z)
$$

$$
\theta_{\phi}=4\sigma(z)
$$

初始化时由网络输出反求：

$$
z=\operatorname{logit}(\theta)
$$

然后仅优化：

$$
z
$$

比较：

$$
C_{\text{Direct-Theta}}
$$

与

$$
C_{\text{Direct-Logit}}
$$

#### 判断

如果 Direct-Logit 仍能从测试集网络输出提升约 40～60 bits，则可以基本排除 Sigmoid 是主要瓶颈。

如果 Direct-Theta 可以大幅提升，而 Direct-Logit 基本无法移动，则说明：

$$
\boxed{\text{Sigmoid 饱和是重要原因}}
$$

---

### Test 3：比较参数空间和 Logit 空间的梯度强度

对训练集和测试集分别计算：

$$
\left\|
\frac{\partial L}{\partial\Theta}
\right\|
$$

以及：

$$
\left\|
\frac{\partial L}{\partial z}
\right\|
$$

重点比较训练样本和测试样本。

如果测试样本满足：

$$
\left\|
\frac{\partial L}{\partial\Theta}
\right\|
\gg0
$$

但：

$$
\left\|
\frac{\partial L}{\partial z}
\right\|
\approx0
$$

则说明输出约束层显著阻断了有效梯度。

---

### Test 4：Per-Sample Gradient Conflict

若 Sigmoid 问题被排除，则进一步检查共享网络参数是否造成样本间优化冲突。

对多个样本分别计算：

$$
g_i=
\frac{\partial L_i}{\partial\Theta_i}
$$

计算梯度平均抵消比例：

$$
R=
\frac{
\left\|
\frac{1}{N}\sum_i g_i
\right\|
}{
\frac{1}{N}\sum_i\|g_i\|
}
$$

并计算样本间梯度余弦相似度：

$$
\cos(g_i,g_j)
=
\frac{
g_i^Tg_j
}{
\|g_i\|\|g_j\|
}
$$

如果大量样本梯度方向差异明显甚至相反，则说明：

> 每个样本在参数空间中都有明确的优化方向，但共享网络参数难以同时满足这些相互冲突的更新需求。

---

## 推荐测试顺序

1. **Direct-Logit vs Direct-Theta**
2. **比较梯度范数**
3. **Per-Sample Gradient Conflict**
4. 若以上均不能解释问题，再进一步研究网络 Jacobian 和输出参数化形式

现阶段暂不建议继续调整 Transformer 深度、Head 规模或 Batch Size，因为已有实验表明这些因素并不是当前问题的主要矛盾。
